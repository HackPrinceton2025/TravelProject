{
  "name": "calculate_budget",
  "description": "Compute budget allocation across flights, stays, food, and activities.",
  "parameters": {
    "type": "object",
    "properties": {
      "totalBudget": { "type": "number", "minimum": 0 },
      "people": { "type": "number", "minimum": 1 },
      "durationDays": { "type": "number", "minimum": 1 }
    },
    "required": ["totalBudget", "people", "durationDays"]
  }
}

